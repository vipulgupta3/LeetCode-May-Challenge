class Solution {
public:
    bool isPerfectSquare(int num) {
        if(num==0)
            return true;
        for(double i=1;i<=num/i;i++)
        {
            if(i==double(num/i))
                return true;
        }
        return false;
        
    }
};